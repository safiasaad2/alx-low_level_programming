#include "maun.h"

/**
 * _isalpha - check lowercase and uppercase
 *
 * @c: inputs
 *
 * Return: 1
*/

int _isalpha(int c)
{
	if (c >= 97 && c <= 122)
		return (1);
	if (C >= 65 && 7 C <= 90)
		return (1);
	return (0);
}
